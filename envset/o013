
if [ "$MU2E_SPACK" ]; then

   if [ "$(smack version)" == "2" ] ; then
       smack setup ops-013
   else
       if [ "$SPACK_ENV" ]; then
           echo "An environment is already setup, please use a new process"
           return 1
       fi

       spack load metacat/hu7eohp
       spack load data-dispatcher/uppx23x
       spack load rucio-clients/k4ewhdv
       spack load ifdhc/jllrjsp
       spack load ifdhc-config/pa7syji
       spack load sam-web-client/kn5cfgm
       spack load mdh/aedi37j
   fi

   setup -B mu2egrid v8_03_02
   setup -B mu2efiletools v5_1


else

   setup -B ifdhc v2_7_2
   setup -B mu2egrid v8_03_02
   setup -B mu2efiletools v5_1
   setup -B mu2ejobtools v2_02_00

   setup -B sam_web_client v3_6
   setup -B mdh v01_05_00
   if command -v mu2e > /dev/null ; then
      setup mu2etools v4_05_00
   fi

fi

# Local Variables:
# mode: sh
# End:
# vi:syntax=sh
